{
  "name": "metaphor",
  "version": "0.10.1",
  "description": "An elegantly crafted UI Kit for CSUN branded prototypes & applications",
  "scripts": {
    "build": "babel src -d lib"
  },
  "devDependencies": {
    "babel-cli": "^6.16.0",
    "babel-core": "^6.17.0",
    "babel-preset-es2015": "^6.16.0",
    "babel-register": "^6.16.3",
    "gulp": "^3.9.1",
    "laravel-elixir": "^6.0.0-11",
    "laravel-elixir-scss-lint": "^1.3.1",
    "laravel-elixir-webpack-official": "^1.0.8"
  },
  "scripts": {
    "deploy": "cd docs && gulp && jigsaw build production &&  cd .. && git add docs/build_production && git commit -m 'Build for production' && git subtree push --prefix docs/build_production origin gh-pages"
  }

}
